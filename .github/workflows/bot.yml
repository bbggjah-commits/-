name: Telegram Bot 24/7

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 تحميل الكود
      uses: actions/checkout@v3
      
    - name: 🐍 إعداد Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: 🚀 تشغيل البوت
      run: |
        echo "🔧 بدء تشغيل البوت..."
        # التوكن مباشرة في الكود (للتجربة فقط)
        python bot.py
